
   
1. List 5 difference between Browser JS(console) v Nodejs
      1) Browser JS is usefull for only frontend but even though NodeJS is predominantly used in backend it can be used in both back and            frontend of development.
      2) NodeJS has full access to the system whereas Browser JS has only limited access to the browser.
      3) Browser JS is capable to add HTML in the script whereas NodeJS can't.
      4) Browser JS can run on almost any engine while NodeJS can only run on chrome v8 engine.
      5) NodeJS is without UI but Browser JS has UI.
      
2. watch & summary 5 points - https://www.youtube.com/watch?v=SmE4OwHztCc&ab_channel=JSConf
      1) It takes 5 steps for a browser to render a webpage.
            1) Parse HTML.
            2) Parse CSS.
            3) Render Tree.
            4) Layout.
            5) Paint.
      2) JS can affect HTML and CSS at the same time.
      3) When it comes to render tree there are multiple trees like 
            1) Render Objects.
            2) Render Styles.
            3) Render Layers.
            4) Line boxes.
      4) Layout - Its simple as its name.
      5) Panit - Its the same.
            
4) The page delivered both an 'X-Frame-Options' header and a 'Content-Security-Policy' header with a 'frame-ancestors' directive. Although the 'X-Frame-Options' header alone would have blocked embedding, it has been ignored.
typeof(1)
"number"
typeof(1.1)
"number"
typeof('1.1')
"string"
typeof(true)
"boolean"
typeof(null)
"object"
typeof(undefined)
"undefined"
typeof([])
"object"
typeof({})
"object"
typeof(NaN)
"number"
